import type React from "react";
type Props = {
    children: React.ReactNode;
    isOpen: boolean;
    onClose: () => void;
};
export declare const ModalWrapper: ({ children, isOpen, onClose }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
